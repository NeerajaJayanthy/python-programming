# Write a Python program to print the prime numbers for a user provided range
st = int(input())
en = int(input())
for i in range(st,en+1):
  if i > 1:
    for j in range(2,i):
      if (i%j) == 0:
        break
    else:
      print(i)
